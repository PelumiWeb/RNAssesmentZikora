# Zikora runs entirely against an in-app mock service (src/services/mock).
# There is no backend and no secret to supply. This file documents the shape a real
# integration would take; copy to .env.local (gitignored) if you wire one up.

EXPO_PUBLIC_API_BASE_URL=https://api.example.invalid
